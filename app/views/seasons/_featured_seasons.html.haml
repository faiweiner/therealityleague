.panel.panel-default
	.panel-body
		.col-md-12
			%h2 Featured Shows
			%p Here are some of the most popular shows at the moment:
			.row
				- @featured_seasons.each do |season|
					.col-sm-4.col-md-3.col-lg-6
						.thumbnail
							= link_to image_tag(season.image), season_path(season.id)
							.caption
								%h4
									= season.show.name + ": "
									= season.name
								%p
									Starts: #{season.premiere_date.strftime("%B %d")}
			= link_to('Browse All Shows and Seasons', seasons_path, class: "btn btn-default btn-lg")