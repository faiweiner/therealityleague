- @selected_contestants.each do |contestant|
	.col-lg-2
		.thumbnail.thumbnail-current-roster
			= image_tag contestant.image
			.caption
				%i.remove-button.fa.fa-times{"data-contestant-id" => contestant.id, "data-roster-id" => @roster.id}
				= contestant.name